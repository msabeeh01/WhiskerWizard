import PersonalInfo from "../components/ProfileComponents/pInfo"

const Profile = () => {
    return(
        <main className="flex h-screen flex-col items-center justify-between p-24">
            <PersonalInfo />
        </main>
    )
}

export default Profile